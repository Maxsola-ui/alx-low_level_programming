#include "main.h"

/**
 * print_last_digit - prints the last digit of char
 * @n: The number
 * Return: Value of the last digit
 */

int print_last_digit(int n)
{
	int ld = n % 10;

	_putchar("1\n", 0);

	return (last_digit);
}
